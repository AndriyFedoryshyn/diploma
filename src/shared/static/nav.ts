import { PATHS } from '../enums/paths';

import { NavItems } from '../types/NavType';

export const nav: NavItems = [
  {
    linkId: 1,
    label: 'Перейти на Доманшю сторінку',
    href: PATHS.HOME,
  },
];
